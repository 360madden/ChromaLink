ChromaLink HTTP Bridge

This service exposes the rolling live telemetry snapshot over localhost.

Endpoints:
- /latest-snapshot
- /snapshot
- /health
- /ready

Default base URL:
- http://127.0.0.1:7337/
